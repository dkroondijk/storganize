<% the_box = defined?(box) ? box : @box %>
<%= simple_form_for @item, url: locker_box_items_path(@locker, the_box, @item), remote: true do |f| %>
  <%= f.input :name, label: false %>
  <%= f.submit "Add Item", id: "add-item-btn" %>
<% end %>